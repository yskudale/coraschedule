// ============================================
// declarations.d.ts
// Vite's built-in types (vite/client, referenced
// in vite-env.d.ts) already cover .scss, .module.scss,
// .png, .jpg, .jpeg, .svg, .webp — so this file is
// now empty on purpose. Add ONLY module declarations
// for file types Vite does NOT already provide types for.
// ============================================

export {};